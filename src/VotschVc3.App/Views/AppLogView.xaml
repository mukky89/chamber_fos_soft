<UserControl x:Class="VotschVc3.App.Views.AppLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="6,2,6,8">
            <StackPanel Orientation="Horizontal">
                <Button Content="← Domov" Command="{Binding DataContext.GoHomeCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
                <TextBlock Text="App log (diagnostika)" FontSize="17" FontFamily="Segoe UI Semibold"
                           VerticalAlignment="Center" Margin="10,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Vyčistiť pohľad" Command="{Binding ClearViewCommand}" />
                <Button Content="Obnoviť" Style="{StaticResource AccentButton}" Command="{Binding RefreshCommand}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <DataGrid ItemsSource="{Binding Entries}" IsReadOnly="True" FontFamily="Consolas">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Čas" Width="170" Binding="{Binding TimestampText}" />
                    <DataGridTemplateColumn Header="Úroveň" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding LevelText}" FontFamily="Segoe UI Semibold"
                                           Foreground="{Binding Level, Converter={StaticResource LogLevelToBrush}}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Zdroj" Width="200" Binding="{Binding Source}" />
                    <DataGridTextColumn Header="Správa" Width="*" Binding="{Binding Message}" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
