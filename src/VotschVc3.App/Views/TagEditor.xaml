<UserControl x:Class="VotschVc3.App.Views.TagEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <StackPanel>
        <!-- Current values as removable chips -->
        <ItemsControl ItemsSource="{Binding Items, ElementName=Root}" Margin="0,0,0,4">
            <ItemsControl.Style>
                <Style TargetType="ItemsControl">
                    <Style.Triggers>
                        <!-- Hide the chip row entirely when there are no values. -->
                        <DataTrigger Binding="{Binding HasItems, ElementName=Root}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ItemsControl.Style>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{Binding Converter={StaticResource TagBrush}, ConverterParameter=bg}"
                            BorderBrush="{Binding Converter={StaticResource TagBrush}, ConverterParameter=border}"
                            BorderThickness="1" CornerRadius="4" Padding="8,2,4,2" Margin="0,0,5,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding}" FontSize="12" VerticalAlignment="Center"
                                       Foreground="{Binding Converter={StaticResource TagBrush}, ConverterParameter=fg}" />
                            <Button Content="✕" Tag="{Binding}" Click="Remove_Click"
                                    Style="{StaticResource GhostButton}" Padding="5,0" Margin="4,0,0,0"
                                    FontSize="11" Foreground="{StaticResource MutedBrush}"
                                    ToolTip="Odobrať" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Add: pick an existing value, or type a new one and press Enter / + -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="SuggestBox" Grid.Column="0" Height="32" ItemsSource="{Binding Suggestions, ElementName=Root}"
                      SelectionChanged="Suggest_SelectionChanged"
                      ToolTip="Vybrať existujúcu hodnotu." />
            <TextBox x:Name="NewBox" Grid.Column="2" Height="32" VerticalContentAlignment="Center" KeyDown="NewBox_KeyDown"
                     ToolTip="Napíš novú hodnotu a stlač Enter (alebo +)." />
            <Button Grid.Column="4" Height="32" Content="＋" Click="Add_Click" Padding="12,0"
                    ToolTip="Pridať napísanú hodnotu." />
        </Grid>
    </StackPanel>
</UserControl>
